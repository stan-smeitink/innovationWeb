const Home = {
  text: 'Home',
  link: '/home',
  icon: 'icon-home'
};

const Login = {
  text: 'Login',
  link: '/login',
  icon: 'icon-login'
};

const headingMain = {
  text: 'Main Navigation',
  heading: true
};

export const menu = {
  headingMain,
  Home,
  Login
}
